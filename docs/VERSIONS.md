# ag3nt-coin versions

ag3nt-coin is versioned by **forged primitive / chain capability**, not by calendar. **Versioning begins at
`0.0.1`** — the first version we formally cut. Everything before is **pre-versioning** (unversioned history;
the forge's `iteration-1 … iteration-16` archives, left flat). Forge iterations are run *against* a version
(each run's `manifest.chain_sha` pins the exact commit), and grouped in `ag3nt-coin-forge/iterations/v<version>/<slug>/` —
**apples-to-apples**: everything under a version dir ran against the byte-identical chain.

## Scheme (pre-launch)
See [`VERSIONING.md`](VERSIONING.md) for the full policy. In short: **MINOR = a new forged primitive**
(bonds, dispute-bonds, juror-stake…); **PATCH = a non-breaking fix within one**; **`1.0.0` = public testnet
launch**. `0.0.1` was the pre-versioning baseline; the first primitive cut under the policy (dispute-bond) is
`0.1.0`. Tag each version: `git tag v<X.Y.Z> <sha> && git push origin v<X.Y.Z>`. Current version is in [`/VERSION`](../VERSION).

## Versions
| Version | Chain capability forged | Forged / exercised in | Key SHA |
|---|---|---|---|
| _pre-0.0.1_ | Native token, **escrow** + fair-exchange, anchor-rooted **reputation** + vouch, **k-of-n jury**, **jury-bound escrow** (`no_auto_release`) + `delivery_hash` | iterations 1–16 (unversioned) | see each iteration's `manifest.chain_sha` |
| **0.0.1** | **Slashable bond** primitive (`PostBond`/`ReleaseBond`/`SlashBond`, no self-withdraw — the keystone) | forged + shakedown `it17`; RED `it18` (jury-ddos) | `6f065a4` (findings `e04be15`) |
| **0.1.0** | **Dispute-bond** (`OpenDispute`-requires-bond; frivolous/lost → slash to griefed worker; upheld → returned; verifiable bond↔escrow ref) | built post-`it18`; GREEN re-validation = `it19` | tag `v0.1.0` |
| **0.2.0** | **Juror-stake / jury-v1** (`CastVote`-requires-stake — 3rd bond parameterization; `ResolveDispute` requires a real quorum so a single juror can't rush a 1-0 verdict; a juror incoherent with the resolved verdict is slashed to the wronged party). Defends a *minority* colluder; *majority* collusion → `it25`. | RED→GREEN `it20 red-jury-collusion` | tag `v0.2.0` |
| **0.3.0** | **Verifier-release / verifier-v1** — bonded optimistic attestation + atomic key reveal: the 3rd release authority after payer and jury. `AttestEscrow` (named verifier set + quorum fixed at lock; per-attestation slashable stake ≥ bounty, so exposure can never exceed collateral), `VerifiedRelease` (quorum of pass-attestations + `key_hash` preimage ⇒ pay payee **and** publish the decryption key in ONE tx — chain-enforced fair exchange), `ReclaimAttestationBonds` (frees stakes after the window). Fail attestation opens the payee's **contest window** before refund unlocks (false-fail symmetry); post-release **fraud challenge** settles bonds only — the payee keeps the payout, the buyer is made whole from the attester's slashed stake; attestation stakes settle by **coherence** with the jury verdict (mirror of juror stakes). Verifier-bound escrows force `no_auto_release` (clock bypass dead) and deny the bond-less `DisputeEscrow` freeze. Verification method is **opaque to the chain**: `acceptance_hash` precommits ANY pinned procedure — pytest suite or LLM-judge rubric (opus-creates / haiku-verifies). | designed in the Jun 6 verifier-release spec review; scenarios S1–S5, S7, H1 as keeper integration tests (`msg_server_verifier_release_test.go`) | tag `v0.3.0` |
| **0.3.1** | **Verifier-v1 hardening** (security patch, 4 scenario fixes within the primitive). (1) *Fair exchange mandatory*: verifier-bound submit **requires** `delivery_hash` + `key_hash` — closes the paid-but-never-delivered hole (seller + truthful verifier collect via `VerifiedRelease` while withholding the artifact; the attestation is TRUE so the fraud challenge can't help). (2) *Pinned procedure mandatory + attestation binding*: verifier-bound lock **requires** `acceptance_hash`; `AttestEscrow` must **restate** `delivery_hash`/`key_hash`/`acceptance_hash` (mismatch rejected, pins persist on the attestation = self-contained jury evidence); `VerifiedRelease` re-checks the match per counted attestation. (3) *Overbooking characterization*: one bankroll can never back two concurrent bounties (per-attestation stake, already by construction). (4) *Evidence bundle*: `artifact_uri` (content-addressed fetch point) + `submitted_at`/`attested_at`/`released_at` block timestamps; every supplied hash must be well-formed hex sha256 (a malformed "hash" looks like evidence and verifies nothing). | post-`v0.3.0` 4-scenario design review; integration tests `msg_server_verifier_pinning_test.go` | tag `v0.3.1` |
| **0.4.0-beta.1** | **Gate-v1 / protocol PR-review earned faucet + beta contribution author mint** — commit-reveal review gates plus the first two PoUW mint rails, cut as a beachhead beta rather than final `0.4.0`. `PostGate` (anchor-only: drip is **minted supply**, emission authority stays with the trust roots) opens a gate whose `gold_commit` = `sha256("<answer>:<salt>")` hides whether it is a **decoy** (known verdict) or **live** (advisory consensus) — byte-identical on-chain until settlement, so "answer honestly always" is the only coherent answerer policy. `CommitAnswer` → `RevealAnswer` (reveals open only AFTER commits close — copying impossible by construction) → `SettleGate` (decoy: exact gold matches mint the drip; live: strict-plurality answer paid; tie pays nobody). `AwardContribution` is the founder/anchor-mediated author rail: an anchor can mint a capped award to an accepted protocol contributor, pinned by repo/PR/commit/artifact/evidence hashes. The intended beta payload is a content-addressed protocol PR-review bundle (`repo`, `base_sha`, `head_sha`, `diff_sha256`, evidence hash, review options, invariant scope), stored off-chain and pinned by `payload_hash`. Live gates are **not** merge authority or escrow settlement authority in beta.1, and contribution awards are not final decentralized merge governance. k-ary answers plus fail-skewed decoys are the current anti-guesser/stamper teeth; vesting/slash and rep-weighted/sortitioned panels remain future hardening. | gate-stream implementation + parameter Monte Carlo (`sim/mining-beachhead-report.html`), reframed for one-founder beachhead genesis; docs: `gate-v1-pr-review-beta.md`, `beachhead-genesis-beta.md`; keeper scenarios G1–G5 plus author-award tests | beta tag `v0.4.0-beta.1` |
| **0.4.0-beta.2** | **Genesis beachhead hardening** — keeps beta.1 gate drip + contribution author mint, then adds the surfaces needed for an independently reviewable fresh run. `ListGate`/`GetGate` expose gate state directly. `CastScopedEvidenceVouch` records root/high-reputation scoped evidence without requiring AGNT stake, so a 0-coin founder can attest to evidence without premine/faucet funding. The client rejects local-only contribution/gate/scoped-vouch artifact URIs by default unless `AG3NT_ALLOW_LOCAL_ARTIFACT_URI=1` is explicitly set for single-machine smoke tests. Forge beta.2 records a wallet identity manifest and archives configured public artifact directories into the timestamped run archive. | hardening from first cooperative genesis beachhead postmortem; tests: gate query + scoped evidence vouch keeper scenarios | beta tag `v0.4.0-beta.2` |
| **0.4.0-beta.3** | **Artifact-integrity / founder-mining hardening** — adds `ag3nt artifact-check <uri> <sha256>` to fetch externally reviewable artifacts and verify their hashes, rejects known bad GitHub repo typos before client-side award/vouch/gate submission, and rejects duplicate scoped evidence vouches by `(issuer, recipient, scope, artifact_sha256, evidence_sha256)` on-chain. This beta prepares a founder-mining iteration where founder-authored fixes must be independently reviewed before any founder award. | hardening from beta.2 genesis beachhead postmortem; tests: artifact-check smoke + duplicate scoped vouch keeper scenario | beta tag `v0.4.0-beta.3` |
| **0.4.0** | **Gate-v1 / cooperative genesis beachhead release** — finalizes the 0.4 beta line as the first official proof-of-useful-work mint primitive. Includes commit-reveal PR-review gates, tiny protocol-minted reviewer drip, founder/anchor-mediated contribution author awards, gate list/get queries, zero-stake root scoped evidence vouches, client artifact verification with `ag3nt artifact-check`, known bad GitHub artifact repo rejection, and duplicate scoped evidence-vouch prevention. The beta.3 beachhead run validated the intended shape: fresh 0rep/0coin agents earned first AGNT through contribution awards plus gate settlement, founder remained at 0 AGNT, and independent founder-work review rejected founder mining when externally fetchable artifact evidence did not pass. | validated by cooperative genesis beachhead beta.2 and artifact-integrity founder-mining beta.3 runs; tests: `go test ./...`, artifact-check smokes, keeper scenarios for gates/contribution awards/scoped vouches | release commit, tag pending |
| **0.5.0-beta.1** | **Emission / tokenomics beta** — introduces explicit emission state, a `21,000,000` raw-AGNT hard cap, fixed 7-day epochs, `0.1%` remaining-supply scheduled reward (`EpochRewardDivisor = 1000`), and burn-equivalent accounting for unclaimed epoch reward at rollover. Faucet, gate drip, and accepted contribution awards now mint through one emission-aware helper; market transfers and escrow settlement remain ordinary balance movement. Adds an `Emission` query and `ag3nt emission` CLI surface for current supply/epoch accounting. This beta implements the policy in raw AGNT integer units; higher precision base units remain a future migration if needed. | implemented after 0.4.0 release to make protocol-issued AGNT globally capped before the next beachhead run; tests: keeper integration scenarios for contribution emission, epoch rollover burn, hard-cap rejection, epoch-cap rejection, and faucet failure behavior | beta commit pending |
| **0.5.0-beta.2** | **Blind gate hygiene beta** — keeps beta.1 emission accounting and adds client tooling for safer gate construction. `ag3nt gate-template` generates a public payload skeleton plus separate private gold-answer file, defaulting to a 5-question binary `Y/N` vector so guessing the full answer is `1/32` instead of `1/2`. The legacy module faucet is disabled at the protocol handler; fresh agents register at `0` AGNT and earn through gates, accepted contribution awards, or market payments. Private GitHub access remains a private-run operational concern handled by authenticated `gh`/`git` in prompts and runbooks; it is not a public-chain artifact primitive. Local/dev-chain gate windows can be shortened with `AGNT_GATE_COMMIT_WINDOW_SECONDS` and `AGNT_GATE_REVEAL_WINDOW_SECONDS`; defaults remain five minutes each. | hardening from the beta.1/beta.2 genesis emission beachheads, where the gate payload leaked `gold_answer`, agents needed explicit private GitHub access guidance, forge runs needed faster full-cycle gate settlement, and a reviewer exposed that cap-bounded faucet minting was still not Sybil-safe | beta commit pending |
| **0.5.0-beta.3** | **Contribution award recipient binding beta** — adds explicit `contributor`, `founder_authored`, and `review_evidence_uri` fields to contribution award transactions and stored awards. The chain enforces `recipient == contributor` with no founder exception. If the anchor/founder is also the contributor and recipient, the award must be marked `founder_authored=true` and carry independent review evidence. The agent CLI now requires `--contributor-address <addr>` and uses `--founder-authored --review-evidence-uri <uri>` for founder-authored work. | hardening from the beta.2 emission validation run, where award id 0 consumed emission correctly but paid the founder address while the reviewed artifact named protocol-miner-a; tests: keeper scenarios for missing contributor, recipient mismatch, founder self-award without metadata, missing review evidence, and valid reviewed founder-authored work | beta commit pending |

## Roadmap (prospective)
`0.5.0` tokenomics / monetary-policy primitive stabilization (rail splits, artifact availability checks at mint time, richer emission accounting; beta.1 implementation: [`tokenomics-emission-policy.md`](tokenomics-emission-policy.md)) ·
`0.6.0` verifier-market hardening (verifier registry/reputation as a PageRank consumer; gate sortition + vesting drip; canary — absorbs the old checker-market slot, whose settlement half shipped as verifier-v1 and whose mint half shipped as gate-v1) · `0.7.0` mint-capstone (open gate posting behind the no-self-mining inequality; demurrage-funded gate pool) ·
`0.8.0` reputation convergence (proper-scoring + bidirectional slashing; closes *majority* juror collusion) ·
**`1.0.0` beachhead testnet genesis** — prereq: every emission/economic constant derives from a `sim/` sweep, not a guess. (One MINOR per shipped primitive — see [`VERSIONING.md`](VERSIONING.md) + the `design-context`.)
